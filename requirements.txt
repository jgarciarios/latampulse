# LatamPulse — dependencias del pipeline
# Día 1 (extracción)
requests>=2.31.0
pandas>=2.0.0
openpyxl>=3.1.0        # lectura de Excel (INDEC/DANE/INE)
beautifulsoup4>=4.12.0 # research manual asistido / fuentes sin API
odfpy>=1.4.1            # lectura de .ods (INE Uruguay)

# Día 3-6 (transformación + análisis)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # driver de Postgres

# Día 7-8 (visualización)
streamlit>=1.32.0
plotly>=5.20.0
jupyter>=1.0.0
matplotlib>=3.8.0
nbformat>=5.9.0
pytest>=8.0.0
